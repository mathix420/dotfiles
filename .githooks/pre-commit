#!/bin/sh
set -eu
GITLEAKS_BIN=${GITLEAKS_BIN:-gitleaks}
if ! command -v "$GITLEAKS_BIN" >/dev/null 2>&1; then
    echo 'Install gitleaks before committing dotfiles.' >&2
    exit 1
fi
exec "$GITLEAKS_BIN" git --pre-commit --staged --redact --no-banner
